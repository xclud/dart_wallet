library wallet;

/// Represents a private key.
class PrivateKey {
  /// Creates a random private key.
  PrivateKey.random();
}

/// Represents a public key.
class PublicKey {
  /// Private contructor.
  PublicKey._();
}
